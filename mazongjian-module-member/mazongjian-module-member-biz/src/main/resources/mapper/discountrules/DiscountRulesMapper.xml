<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yanzu.module.member.dal.mysql.discountrules.DiscountRulesMapper">
    <update id="changeDiscountRulesStatus">
        update
            member_discount_rules
        set status=#{status}
        where id = #{id}
    </update>

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="getDiscountRulesPage"
            resultType="com.yanzu.module.member.controller.app.store.vo.AppDiscountRulesPageRespVO">
        select dr.*, s.store_name
        from member_discount_rules dr
        left join member_store_info s ON s.store_id = dr.store_id
        where dr.deleted = 0
        <if test="null!=storeId and storeId!=''">
            and dr.store_id=#{storeId}
        </if>
        order by pay_money

    </select>
</mapper>
