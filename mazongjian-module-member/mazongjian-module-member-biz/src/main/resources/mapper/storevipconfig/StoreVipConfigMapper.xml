<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yanzu.module.member.dal.mysql.storevipconfig.StoreVipConfigMapper">
    <update id="deleteByStoreId">
        update member_store_vip_config
        set deleted=1
        where store_id = #{storeId}
    </update>

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="getVipConfig"
            resultType="com.yanzu.module.member.controller.app.store.vo.AppStoreVipConfigListRespVO">
        select *
        from member_store_vip_config
        where deleted = 0
        and store_id=#{storeId}
        order by vip_level asc
    </select>
    <select id="selectbyStoreId"
            resultType="com.yanzu.module.member.dal.dataobject.storevipconfig.StoreVipConfigDO"></select>
</mapper>
