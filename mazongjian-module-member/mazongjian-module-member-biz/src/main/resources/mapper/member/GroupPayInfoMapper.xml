<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yanzu.module.member.dal.mysql.groupPay.GroupPayInfoMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="getGroupTotalMoney" resultType="java.math.BigDecimal">
        select ifnull(sum(group_pay_price),0)
        from member_group_pay_info where deleted=0
        AND store_id IN
        <foreach collection="collection" item="item" index="index" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>
    <select id="getBusinessStatistics"
            resultType="java.math.BigDecimal">
        select ifnull(sum(group_pay_price), 0) tgMoney
        from member_group_pay_info
        where deleted = 0
        <if test="null!=startTime and null!=endTime">
            and to_days(create_time) >= to_days(#{startTime})
            and to_days(create_time) &lt;= to_days(#{endTime})
        </if>
        <if test="null!=storeId and storeId!=''">
            and store_id=#{storeId}
        </if>
        <if test="null==storeId or storeId==''">
            and store_id in (select store_id from member_store_user where deleted = 0 and user_id = #{userId} AND type IN (12,13))
        </if>
    </select>
    <select id="getByOrderId" resultType="com.yanzu.module.member.dal.dataobject.groupPay.GroupPayInfoDO">
        select *
        from member_group_pay_info
        where order_id = #{orderId}
    </select>
    <select id="getPage" resultType="com.yanzu.module.member.controller.admin.groupPay.vo.GroupPayInfoRespVO">
        select g.*, s.store_name
        from member_group_pay_info g
        left join member_store_info s ON g.store_id = s.store_id
        where g.deleted = 0
        <if test="null!=groupName and groupName!=''">
            and locate(#{groupName},g.group_name)>0
        </if>
        <if test="null!=groupNo and groupNo!=''">
            and locate(#{groupNo},g.group_no)>0
        </if>
        <if test="null!=groupPayType and groupPayType!=''">
            and g.group_pay_type=#{groupPayType}
        </if>
        <if test="null!=storeId and storeId!=''">
            and g.store_id=#{storeId}
        </if>
    </select>
</mapper>
